import { Link } from 'react-router-dom';

// Simple non-animated navbar without any intensive effects
const Navbar = () => {
  return null; // Return null to completely remove the navbar
};

export default Navbar;
